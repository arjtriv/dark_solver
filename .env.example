# Dark Solver (simulation-only) configuration

# Required (set this to a real endpoint)
ETH_RPC_URL="https://mainnet.infura.io/v3/CHANGE_ME"

# Default chain for analyze_target.sh when no CLI chain_id is passed
CHAIN_ID="1"

# Optional
RUST_LOG="info,dark_solver::solver=info"
VERIFICATION_TELEMETRY_DIR="./telemetry"
